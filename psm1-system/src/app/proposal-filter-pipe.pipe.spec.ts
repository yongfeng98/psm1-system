import { ProposalFilterPipePipe } from './proposal-filter-pipe.pipe';

describe('ProposalFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProposalFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
