import { SessionSemesterPipePipe } from './session-semester-pipe.pipe';

describe('SessionSemesterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SessionSemesterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
