import { LecturerFilterPipePipe } from './lecturer-filter-pipe.pipe';

describe('LecturerFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LecturerFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
